export enum MessageType {
  TEXT = 'TEXT',
  FILE = 'FILE'
}

export enum SenderType {
  INCOMING = 'INCOMING',
  OUTGOING = 'OUTGOING'
}

export enum ConversationStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED'
}

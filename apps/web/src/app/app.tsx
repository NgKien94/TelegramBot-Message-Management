import {Card, Empty, Error, Input, Modal, NotFound } from '@message-management/ui'
import { useState } from 'react';

export function App() {
  return (
    <div>
      <NotFound></NotFound>
    </div>
  );
}

export default App;
